//Para concatenar texto y variables de una manera mas sencilla.

export function getSaludo(nombre) {
  return 'Hola ' + nombre + '!!!';
}

//const nombreCompleto = nombre + " " + apellido;

//esta seria la forma sin Template String

//por lo que ahora se usa como:

//const nombreCompleto = `${nombre} ${apellido}`;
// console.log(nombreCompleto);
